plugin: community.vmware.vmware_vm_inventory
strict: False
validate_certs: False
with_tags: true
properties:
  - runtime.powerState
  - config.name
filters:
  - "'ansible_managed' in tags"
hostnames:
  - config.name
